<nav class="navbar navbar-expand-lg sticky-top text-dark" style="background-color:#CD5C5C !important">
  <div class="container"> 
    <a class="navbar-brand text-dark h1 btn rounded" href="{{path('default_home')}}" style="background-color:#E9967A">ACTUNEWS</a>
     <button class="navbar-toggler btn-dark text-dark" style="background-color:#E9967A" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-list" viewBox="0 0 16 16">
  <path fill-rule="evenodd" d="M2.5 12a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5zm0-4a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5zm0-4a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5z"/>
</svg>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link text-dark text-uppercase" href="{{path('default_home')}}">Accueil</a>
        </li>
         {# <li class="nav-item dropdown bg_secondary">
              <a class="nav-link dropdown-toggle text-dark text-uppercase" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                catégories
              </a>
              <ul class="dropdown-menu bg_secondary" aria-labelledby="navbarDropdown" style="background-color:#CD5C5C!important">
                {% for category in categories %}
                  <li><a class="dropdown-item" href="#">
					      {% endfor %}
              </ul>
            </li> #}
        {# {{dump(app.user)}} #}
        {% if not app.user %} {# si app.user est null #}
          <li class="nav-item">
            <a class="nav-link text-dark text-uppercase" href="{{path('register')}}">Inscription</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-dark text-uppercase"  href="{{path('app_login')}}">Connexion</a>
          </li>
        {% else %}
          {# {% if app.user.roles is same as 'ROLE_ADMIN' %} #}
          {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle text-dark text-uppercase" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Administration
              </a>
              <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                <li><a class="dropdown-item" href="{{path('admin_dashboard')}}">Dashbord</a></li>
                <li><a class="dropdown-item" href="{{path('create_article')}}">Créer un article</a></li>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item" href="#">Something else here</a></li>
              </ul>
            </li>
          {% endif %} 
          {{ render(url('render_categories'))}}
          {# <li class="nav-item">
            <a class="nav-link text-dark text-uppercase" href="{{path('contact')}}">Contact</a>
          </li> #}
          <li class="nav-item">
            <a class="nav-link text-dark text-uppercase" href="{{path('app_logout')}}">Se deconnecter </a>
          </li>
        {% endif %}

        
      </ul>

    </div>
  </div>
</nav>
